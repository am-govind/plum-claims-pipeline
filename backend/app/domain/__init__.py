"""Domain layer: pure business logic.

Entities, value objects, aggregates, and domain services live here. This
layer imports nothing from `app.application` or `app.infrastructure`; it
is the stable core that the rest of the system depends on.
"""
